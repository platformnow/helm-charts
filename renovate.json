{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "datasourceTemplate": "docker",
      "fileMatch": [
        "charts/.+/Chart\\.yaml$"
      ],
      "matchStrings": [
        "#\\s*renovate: image=(?<packageName>.*?)\\s+appVersion:\\s*['\"]?(?<currentValue>[\\w+\\.\\-]*)['\"]?"
      ]
    },
    {
      "customType": "regex",
      "datasourceTemplate": "helm",
      "fileMatch": [
        "charts/.+/Chart\\.yaml$"
      ],
      "matchStrings": [
        "^name:\\s*(?<depName>[^\\s]+)\\s*[\\s\\S]*?^version:\\s*['\"]?(?<currentValue>[\\d\\.]+)['\"]?\\s*$"
      ],
      "depNameTemplate": "{{depName}}"
    }
  ],
  "packageRules": [
    {
      "matchDatasources": ["helm", "docker"],
      "groupName": "helm-chart-releases",
      "commitMessageExtra": "update {{{depName}}} chart {{{newVersion}}} and appVersion {{{newValue}}}"
    },
    {
      "matchDatasources": ["helm"],
      "matchFileNames": ["charts/.+/Chart\\.yaml$"],
      "matchUpdateTypes": ["patch", "minor", "major"],
      "groupName": "helm-chart-releases",
      "commitMessageExtra": "update {{{depName}}} chart {{{newVersion}}} and appVersion {{{newValue}}}"
    }
  ]
}
