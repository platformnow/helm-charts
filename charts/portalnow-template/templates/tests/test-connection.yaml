apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portalnow-template.fullname" . }}-test-connection"
  labels:
    {{- include "portalnow-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portalnow-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
